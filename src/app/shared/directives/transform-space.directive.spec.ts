import { TransformSpaceDirective } from './transform-space.directive';

describe('TransformSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
